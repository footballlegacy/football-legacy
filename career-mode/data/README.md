# Club Database

Edit `Football_Legacy_All_Clubs_Master.xlsx`, then run:

```bash
python3 tools/build_club_database.py
```

Do not manually maintain `club-database.json` or `js/club-database.js`; both are generated from the spreadsheet.
