# FL-MSQ4R4TZ restart, contact, keeper, and crossing audit

Scope: read-only runtime audit of engine `0.174`, effective authority `fl-v2-live`. Event references are from the user-supplied `football-legacy-playtest-FL-MSQ4R4TZ.json`. Runtime source was not edited.

## Findings

| Note | Exact event evidence | Current root cause/status | Surgical fix and acceptance test |
|---|---|---|---|
| 4: CPU run/out | `46` Costa ground-passes to Hazard from `(1247.9,1180)` toward `(1217.5,759.8)`; no reception follows. The loose ball reaches `(1163.2,-26)` and becomes a home throw at `53`. | This is an intended-receiver/recovery failure, not a logged CPU carrier deliberately running out. Current V2 now gives the intended receiver a 1.35s ETA preference (`live-v2-authority-adapter.js:568-611`), so it is plausibly mitigated but has no replay proof. | Replay an open, on-pitch ground pass near the line. Assert target ETA decreases and either target reception or an explicit `receiver-abort` precedes out-of-play. |
| 7: throw aim/rotation | `81` stages Ashley Cole's throw; `88` launches to Vieira immediately after resume. | Rotation is applied from aim every held frame (`match.html:4703-4706`), but the guide is deliberately visible for every local restart except free kicks (`6657-6669`). The unwanted guide is still current behavior. | Hide/minimalize the throw guide after orientation is established; add a render-state assertion for throw-hold plus a facing-vector assertion. |
| 13: awful CPU throw | `122` targets Pedro at `(2740.6,1891)` from `(2705.6,2168)`. At `126` the ball is already `(2814.4,1387)`, about 506 units beyond the intended Y; no Pedro reception occurs. After Gilberto receives and shoots, keeper events `141-142` still identify the flight as `throw-in`. | CPU throws use a constant `6.0/3.2` launch regardless of target distance (`2713-2717`), so the solver does not land at the chosen receiver. The exported sequence also proves stale flight metadata survives possession/shot. Still actionable. | Solve speed/loft from distance, clamp a legal throw range, and clear pass-flight metadata at first controlled touch. Assert landing error <= one control radius and that a subsequent shot cannot have `flightType=throw-in`. |
| 18: static CPU fullback | `190` Courtois keeper-passes toward Marcos Alonso `(2257,273.4)`; no Alonso reception occurs. The ball remains loose until `199`, 419 frames later, when Azpilicueta receives at `(2273.4,285.7)`. | Same intended-receiver chase failure as note 4. Current V2 ETA weighting is a partial mitigation; no proof it owns this keeper-pass case. | Keeper-pass scenario: intended fullback must close on the projected ball and receive before an unassigned teammate, or emit a reasoned abort/reassignment. |
| 21/22: throw and pause auto-take | `241` stages Lauren's throw. `247` pauses at real time `1786542361854`; `249` auto-launches at the identical match frame after a 19.46s wall-clock pause, with no Cross/pass input. | Deterministic current bug. Pause shifting excludes `restartStartedAt`, `restartEarliestAt`, `restartForceAt`, `restartReadySince`, and `resetUntil` (`6786-6788`). The non-kickoff 12s watchdog then unconditionally calls `aiRestartPass` for the human taker (`4715-4718`). | Shift all restart timestamps by pause duration (or use simulation time) and gate watchdogs with `!teamHasHumanController(restartTaker.team)`. Pause 20s on a held human throw; after resume assert no pass until a fresh release edge. |
| 24/29/35: slide hitbox | Slides `258/259 -> 278`, `352/353 -> 375`, and `509/510 -> 533` all end `won:false`. At note 29 the second slide finishes only 11.4 units from the loose ball. The supposedly successful note 35 is actually a later `reception` (`534`) and `possession-gained` (`537`). | Not a radius-tuning problem. Both slide collision paths only inspect `ball.owner` (`3015-3027`, `3086-3089`), so a slide cannot hit a loose ball. The V2 snapshot maps only `lunge` to `tackleActive` (`1816`), and the adapter emits only `stand-tackle` (`live-v2-authority-adapter.js:615-632`). | Add an owner-independent swept ball-contact phase (previous-to-current ball and tackler segments/capsule), then a carrier/foul phase. Expose a distinct `slide-tackle` intent to V2 and resolve exactly once. Recreate all three trajectories; the successful feel case must log slide contact before reception, not `won:false` plus incidental pickup. |
| 26: keeper Cross release | Keeper ownership begins `291` at frame 3885 and auto-distribution occurs `325-326` at frame 4184: 299 frames. No Cross controller event exists in the interval. | The recorded release is the hard 300-frame keeper watchdog (`4556-4561`), not a rejected logged Cross action. The missing edge could be browser/gamepad sampling or a press consumed while paused; the log cannot distinguish it. | Log raw edge plus `keeper-release accepted/rejected` reason. End-to-end test Cross down/up during keeper possession, including held-across-pause and reconnect cases; require exactly one release before watchdog. |
| 30: glide and goal-kick staging | At restart `407`, opponents Diego Costa `(126.6,526.2)` and Pedro `(394,1383.2)` are inside the home box. During the same frozen match frame, later events show them at the exclusion boundary `(625.1,...)`. | `restart()` logs before a later update enforces the goal-kick exclusion. `enforceGoalKickExclusion()` hard-moves illegal opponents only after staging (`1960-1964`, called at `4692-4694`). This exposes illegal initial staging and visible dead-ball migration/glide. Still actionable. | Make staging atomic: legalize players and targets before the restart event/camera reveal; clear locomotion/action state at the staging cut. Assert zero opponents in the box on the first observable restart frame and no position changes while the match frame is frozen. |
| 39: keeper carry arms | Courtois carries from `554` until distribution `622`; the note is `627`. | The pose is fixed arm joint rotations with no hand-to-ball socket (`6521`). CPU keepers get `action=keeperCarry`, but the human keeper path sets only `keeperPose=carry` and never that action (`4560-4561`). Still actionable. | Give both control paths the same carry action; attach the owned ball to a chest/hand socket and animate legs independently. Add pose invariants for hand-ball distance throughout movement and a visual snapshot. |
| 43: R1+Square low cross | `657` correctly logs `action=low-cross`; `658-660` assign five runs and target Henry, so the mapping and receiver selection did work. However it launches at pace `25.53`, speed `24.784`, predicted contact frame `31`; at `664`, age 35, the ball is still at `z=74.6` (declared contact height `13`) and has missed everyone, then exits for a throw. | Input mapping is already fixed. The remaining fault is trajectory/contact calibration: `doLobPassFor` uses the shared cross solver and accepts its high speed/loft (`2875-2893`), while the contact window fires high and late. | Cap low-cross pace, use a flat/skidding profile, and solve contact time/height consistently. Assert contact-frame height near the declared height, arrival within a receiver stride, and no untouched full-width continuation. |

## Priority

1. Pause-safe human restarts (silent loss of control).
2. Owner-independent slide contact plus V2 slide intent (core control contract).
3. Distance-solved throws and metadata clearing.
4. Atomic goal-kick staging.
5. Keeper input telemetry/carry socket and low-cross calibration.
6. Replay proofs for the current intended-receiver mitigation.
