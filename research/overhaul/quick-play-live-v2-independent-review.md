# Quick Play Live FL V2 Independent Promotion Review

Date: 2026-08-12
Scope: explicit offline Quick Play Single Player and all-CPU CPU-versus-CPU
gameplay authority plus the separately composed Set-Piece Suite control route
Verdict on the frozen candidate below: **GO for the three explicit offline FL V2 playtest authority contracts; final Set-Piece Suite browser confirmation remains a release gate**

## Frozen bytes reviewed

- `match-engine/live-v2-authority-adapter.js`: `e8761ae8755001ee847191e92ee93528236059d5124ea90a2f9d4f38d4c1dc80`
- `match-engine/match.html`: `1787fa39b8f2d11954fd58a48257ce885f24492fdd20e5d9e654ae017bf29aa6`
- `match-engine/live-v2-contact-authority-composer.js`: `d7e950dc44e70531ef8a9cff2ef7ffaa0c0779707734428972d58d1414b1292e`
- `match-engine/live-v2-match-control-composition.js`: `ab6b48ab4c032c2f1efadf6c8f30be7d1bbfeba8937bbb249aefa53892c35e58`
- `quick-play/app.js`: `db54a53dcc54c1735b39ace3729f8691682c05a0da11c2270e9ed6918f832178`
- `quick-play/index.html`: `d0a7e9c87060d87e1314b01823300a89088eb8356ae4c73ae86427ec3b80a515`

## Independent automated evidence

The final frozen-byte independent/foundation seal passed **52/52** test blocks.
The focused CPU-versus-CPU authority and Quick Play selection suite passed
**13/13** test blocks, and the canonical match-control suite passed **1,137
assertions**. These include the late-release Ball chronology regression, live
contact composition, match-control composition and the three-route Quick Play
selector/workflow boundary.

The final combined frozen integration sweep passed **170/170** test blocks. It
also covered first touch, aerial contact, period handoff, unified promotion,
controller reconnect, Online input-transport preservation, offside/camera
policy and recent contact/input regressions.

The subsequent `FL-MSPYQGTA` reconciliation retained the same authority
boundary while adding cumulative authority telemetry, a keeper-own-release
recontact exclusion, a compact workflow badge and pause-safe goal presentation.
The focused affected-family rerun is recorded in
`playtest-backlog-reconciliation-FL-MSPYQGTA-2026-08-12.md`; no fallback path
or additional workflow was introduced.

The new gates prove:

1. Exact frozen bytes and exact dependency versions/schemas.
2. Unforgeable capability provenance; online markers and unsupported workflows fail closed.
3. Immutable private plans, exact-once commits, and sequential ticks.
4. Whole-tick host rollback followed by a blocking strict V2 stop on commit failure; the match cannot continue as Build 173.
5. Released-ball Ball V2 authority retains team, intended receiver, and offside context.
6. Deterministic replay, finite bounded telemetry, and reset re-arm at tick one.
7. Exact query/payload/seed preflight; CPU-versus-CPU additionally requires the
   exact external `spectator` route, zero human owners, AI ownership of both
   teams and one `autoplay=1`. Duplicate, zero-seed, online, cooperative,
   human-control, unsupported and shadow-conflict cases load no V2 scripts.
8. Exactly one live V2 tick hook and success-gated overlapping legacy
   locomotion, CPU, contact, loose-ball integration, and goal-frame collision.
   A CPU-versus-CPU tick plans both teams through CPU, movement and formation V2.
9. Existing controller, keyboard, restart, replay, camera, goalkeeper, rules, and red-card containment remains Build 173.
10. Madrid BBC identity is byte-exact and every Quick Play workflow remains present.
11. Circular Build 173 player/scan references from an ordinary pass are captured by identity instead of traversed through JSON.

The focused engine suites additionally prove deterministic beeline runs, formation/rest-defence behaviour, carrier actions, pass-flight support, organic offside, transition phases, first-touch/aerial chronology and replay invariance.

## Closed browser blocker

The first frozen browser candidate fell back after an ordinary human movement/pass sequence. The exact diagnostic was a circular JSON traversal through Build 173 runtime references: `player.scan.teammates[0].player` closed back onto the player. Player runtime fields, `TEAM_BRAIN`, and `actionPowerFeedback.actor` deliberately retain these live references, so the transaction now saves/restores their top-level identities while continuing to clone JSON-safe stats and report data.

The exact prior failing browser sequence was rerun on an earlier frozen Single
Player candidate through `committedTick=818`, closing the circular-reference
blocker. The final match bytes listed above were then verified again through
the real Quick Play surface:

- CPU-versus-CPU reached `committedTick=455`, retained the `fl-v2` authority
  badge, reported `failureCode=none`, and produced zero browser logs.
- Single Player was re-launched after the `FL-MSQ1ILVP` contact/recovery repair,
  reached `candidateTicks=1068` and `committedTick=1068`, reported
  `failureCode=none`, rendered the high-visibility yellow ball, and produced
  zero warning/error browser logs.

The real Quick Play Set-Piece Suite selector was previously proven to launch
the FL V2 Set-Piece Suite, accept take-set-piece input, and open its dedicated
Escape menu without ordinary pause. One final confirmation on the exact final
match hash above remains required before publication.

## Boundaries of this GO

- Build 173 remains the deliberate pre-match default. Candidate transactions still roll back atomically, but an opted-in V2 runtime fault now freezes the match rather than switching gameplay authority.
- Only explicitly selected offline Single Player, exact all-CPU CPU-versus-CPU,
  and the Set-Piece Suite can arm FL V2.
- Online, local two-player, Home Co-op and every other unsupported workflow
  remain Build 173 or fail closed. CPU-versus-CPU without explicit FL V2
  selection also remains Build 173. The shared renderer, goalkeeper,
  normal-match rules and protected replay lanes remain Build 173-owned at the
  documented composition boundary.
- The browser evidence is a promotion smoke/playability gate, not a claim that every match outcome has received human tuning.

No existing workflow was removed or reduced by this review.
