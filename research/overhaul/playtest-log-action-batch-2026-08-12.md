# Three-log playtest action audit — 2026-08-12

Scope: every tester annotation in `FL-MSPJ3TUN` (23), `FL-MSPKF196` (2), and `FL-MSPL675Y` (10). This audit distinguishes an implemented change from a source-level approximation, a positive observation, and work that still requires a real playtest. It does not treat a passing source assertion as visual or hardware proof.

Status key: **done** = an implementation and focused regression exist; **partial** = part of the note is implemented but an exact request or proof remains; **observe** = no change was requested; **manual proof** = implementation exists but the relevant hardware/visual behaviour still needs a real playtest.

## FL-MSPJ3TUN — Single Player

| # | Minute | Tester note / requested outcome | Current implementation evidence | Status |
|---:|---:|---|---|---|
| 1 | 1 | Bluetooth controller was not detected after waking it in Quick Play. | `controller-ui.js` now deletes stale pad state on disconnect/reconnect; Quick Play refreshes its controller card on both events. `tests/controller-reconnect-and-quick-play-launch.mjs`. | **done; manual proof** on the physical Bluetooth DualSense remains. |
| 2 | 1 | Match needed two clicks; first click appeared to load for a long time. | `quick-play/app.js` now makes offline launch single-flight, disables Start immediately, exposes `aria-busy`, paints “Loading Match”, then launches once. Focused controller/launch regression covers this. | **partial**: duplicate/ambiguous clicks are fixed; underlying large-page load duration has not been benchmarked or reduced by this batch. |
| 3 | 1 | Typing tester notes triggered game controls / blocked letters. | `editableInputTarget()` guards both global keydown and keyup before any game-input side effects and recognises textarea/input/contenteditable. `tests/playtest-contact-input-regressions.mjs`. | **done**. |
| 4 | 1 | Bluetooth reconnect produced permanently held D-pad Down; USB restored control. | Raw Firefox DualSense hat input now accepts only the eight real hat values; a reconnect-zero axis packet is neutral, standard-mapped pads cannot also read the raw axis, and pad edge state is cleared on reconnect. `tests/controller-reconnect-and-quick-play-launch.mjs` covers the real `.142857` Down value, neutral `0`, standard mapping, disconnect, and reconnect. | **done; manual proof** remains for the exact Mac Bluetooth lifecycle. |
| 5 | 3 | Short pass ran straight out; concern that passes, free kicks and shots were over-powered. | `doPassForHuman()` now uses hold-to-distance calibration, a lower 4.6–17.2 host pace, and short-tap assistance. Human open-play shots and direct free kicks now use metre-based calibration through `humanOpenPlayShotPaceMetresPerSecond()` and `directFreeKickPaceMetresPerSecond()`: their tested outputs stay inside 22.5–34 m/s (the model clamps open shots to 25–33 m/s and direct free kicks to 27–34 m/s), increase with charge, and publish launch `paceMps` plus distance-based `arrivalMs`. `tests/playtest-replay-keeper-cadence-regressions.mjs`. | **done**. Subjective power feel still belongs in full-match tuning rather than being inferred from one uncaptured incident. |
| 6 | 7 | Pass went in the wrong direction. | Human pass direction now takes the normalised left stick above a deadzone and otherwise the footballer’s facing; target ranking is relative to that vector. | **done in source; manual proof** remains because no focused compass-direction runtime fixture was added. |
| 7 | 10 | Thrower dropped the ball; hold longer; CPU must decide; receivers must create options. | Throw-ins now stage for at least 3 seconds, use a reachable readiness threshold, produce four moving routes, and retain a forced restart deadline. CPU throw-ins select a staged receiver and launch through the common source-lock path. `tests/playtest-contact-input-regressions.mjs`. | **done**. |
| 8 | 11 | Redo throw-ins: rotate left/right and use Cross/X for variable power. | The dead-ball hint and input now use LS to rotate/choose and hold/release Cross/A for power; launch speed and loft scale monotonically with charge. Focused throw-in regression. | **done**. |
| 9 | 15 | Lofted pass “broke”. | Loft/lob launch now uses `standardLobTrajectory()` and release source-locking, preventing the passer from immediately re-contacting its own launch. Focused contact regression covers loft source exclusion; the built-in gameplay self-test covers deterministic lob shape. | **done**. |
| 10 | 21 | Turf drag looked good; pass again had excessive power; slow jog/sprint gait; make off-ball pace slightly higher than dribbling pace by ratings; two-frame directional-touch acceleration; faster/better shield animation; retain skill usefulness. | Pass pace is bounded; Movement V2 gives carriers a small control-scaled 0.4–3.5% run/sprint cost while off-ball pace is unchanged; directional first touch grants a two-tick acceleration-only burst; shield speed was raised to 1.9–3.1 m/s, turn rate improved, and the live projection supplies a mirrored shielding pose. The later strict-V2 log showed that `gaitCadenceScale: 2` read as fast-forward under render catch-up, so presentation is now calibrated to `gaitCadenceScale: 1.2` and `visualStepDistanceRatio: .8333` while `worldSpeedScale` stays exactly `1`. `tests/movement-engine-v2.mjs`, `tests/playtest-contact-input-regressions.mjs`, and `tests/playtest-replay-keeper-cadence-regressions.mjs`. | **done in model; manual proof** remains for the revised animation cadence. |
| 11 | 24 | Loft/cross ballooned beyond intent, although flight looked good. | Lob and cross flight are now distance-calibrated ballistic trajectories; crossing uses a target-contact height rather than an arbitrary launch impulse. Built-in lob/cross self-tests plus focused release-lock regression. | **done in model; manual tuning proof** remains for feel. |
| 12 | 27 | Throw-ins are broken and need an overhaul. | Same full throw-in replacement as notes 7–8. | **done**. |
| 13 | 31 | Add R1 + Square low cross. | Both local controllers route R1 + Square to `low-cross`; it has its own 13-unit contact-height trajectory and receiver-flight classification. Focused regression. | **done**. |
| 14 | 37 | CPU centre-back should chase the ball; L1 should always select closest player to ball. | L1/LB now uses pure distance to the live ball/opposition owner and logs `l1-closest-ball`. `outfieldLooseBallRecoveryContext()` deterministically filters safe goal-side outfielders, ranks their projected-ball ETA, and gives the winning defender an `attack-and-claim` route. The minute-37 regression reproduces a slow loose ball, proves the goal-side centre-back wins over a faster but unsafe full-back, advances the race to an actual `outfield-loose-ball-claim`, and records no difficulty physical bonus. `tests/playtest-replay-keeper-cadence-regressions.mjs`. | **done**. |
| 15 | 38 | Goalkeeper should actively collect; keeper carrying felt extremely slow. | Keeper loose-ball decisions compare keeper, defender and opponent ETA and claim only when the keeper wins safely; the same deterministic outfield recovery contract yields immediately when `goalkeeperLooseBallContext()` reports the keeper wins, with reason `goalkeeper-wins-loose-ball-eta`. Keeper-in-hands carry has an isolated 3.45/3.90 speed floor. `tests/playtest-replay-keeper-cadence-regressions.mjs`. | **done**. |
| 16 | 39 | Defenders crowded the goalkeeper in his box. | Goalkeeper possession now forces buildup phase and release positions: split centre-backs at 500 depth, wide defenders at 585, one midfield outlet at 760; debug contract requires no outfielder within 400 host units. Keeper/formation focused regressions. | **done**. |
| 17 | 45 | Tiny L1 + Triangle tap travelled far; apply collision/hitbox thinking to players. | Triangle is now press/hold/release with a `.10` tap floor and monotonic power; over-the-top distance, lead and ballistic speed scale with charge. Movement V2 uses per-player radii, swept contacts and deterministic mass-weighted player separation in live authority. `tests/playtest-contact-input-regressions.mjs` and `tests/movement-engine-v2.mjs`. | **done**. |
| 18 | 45 | First two foul-replay angles must show the foul, run longer, and play slower. | Ordinary foul replay is 6.2 s (carded 7.0 s, red 7.6 s); its first two anchor windows straddle contact and the replay cursor can seek backwards to the contact frame. `tests/playtest-replay-keeper-cadence-regressions.mjs`. | **done**. |
| 19 | 47 | Through ball was nicely weighted. | Positive observation; the new charge model preserves a bounded range rather than adding an unrelated change. | **observe**. |
| 20 | 48 | Strikers disappeared and the whole side stayed too deep; refine striker/winger behaviour. | Formation V2 now receives live carrier progress. Invincibles settled attack keeps both strikers and wide support on carrier-relative floors; Cole is excluded from rest defence and Lauren forms the asymmetric third defender. `tests/formation-behaviour-v2.mjs`. | **done in contract; manual match proof** remains. |
| 21 | 54 | Repeated request to fix Bluetooth after cable change. | Same reconnect/neutral-packet fix as notes 1 and 4. | **done; manual proof** remains. |
| 22 | 54 | Duplicate/expanded striker-and-winger depth note. | Same Invincibles carrier-relative support fix as note 20. | **done in contract; manual match proof** remains. |
| 23 | 60 | FL V2 had fallen back to Build 173; log should reveal when/why. | V2 period authority now hands half-time, exact second-half start, and full-time to the host without preparing a stale live tick. Log V4 records engine-authority transitions and one structured `v2-authority-stop` with failure code/tick/counters. The strict host rolls back and freezes behind an exportable diagnostic; it cannot continue as Build 173. `tests/live-v2-period-handoff-integration.mjs`, `tests/live-v2-strict-playtest-stop.mjs`. | **done**. |

## FL-MSPKF196 — Set-piece Suite

| # | Minute | Tester note / requested outcome | Current implementation evidence | Status |
|---:|---:|---|---|---|
| 1 | 1 | RS Down should move the camera backward, not forward. | FL V2 Set-piece Suite roaming now scopes RS to camera orbit/dolly before placement: Down increases distance, Up decreases it, Left/Right orbit; 112–330 bounds; no global stick inversion. `tests/set-piece-camera-replay-integration.mjs`. The prior real Set-Piece Suite browser visual smoke was green. | **done**. Subjective stick/camera feel remains a manual tuning question. |
| 2 | 1 | Improve penalty/free-kick camera placement; clean anchored pan; selected shots get three full slow-motion replays from different angles, with the last at half the speed of the first two. | Free-kick camera is placed 238 behind/118 high, penalty 185/90; placement looks at aim, strike hold then tracks the live ball. The replay package freezes one source clip and plays it completely at `.5`, `.5`, `.25` from three distinct angles, with whole-package skip and exactly-once completion for goals, misses, corner saves, free kicks, and penalties. `tests/set-piece-camera-replay-integration.mjs`. The prior real Set-Piece Suite browser visual smoke was green. | **done in contract and prior browser smoke**; final subjective composition tuning remains manual. |

## FL-MSPL675Y — CPU v CPU

| # | Minute | Tester note / requested outcome | Current implementation evidence | Status |
|---:|---:|---|---|---|
| 1 | 4 | Use the requested three-replay package and make it skippable. | Same immutable `.5/.5/.25` three-angle package; Cross/A or Space skips the whole package and returns once. Set-piece camera/replay integration regression. | **done**. |
| 2 | 17 | CPU free kicks looked broken. | CPU now chooses a direct attempt only in its viable range; otherwise it selects a forward/staged receiver and uses `standardLobTrajectory()` from actual service distance, logging `cpu-free-kick-delivery-plan`. | **done in source; manual CPU-match proof** remains because no focused dynamic CPU-free-kick outcome fixture was added. |
| 3 | 37 | HUD said FL V2 unavailable. | Quick Play now permits FL V2 for `spectator`; preflight translates that exact offline all-CPU launch to `cpu-v-cpu`, requires zero human ownership and `autoplay=1`, and gives both teams Ball, Movement, CPU, Formation, First Touch and Aerial authority. `tests/cpu-v-cpu-live-v2-authority.mjs`. A real Quick Play CPU-v-CPU browser run retained the `fl-v2` badge through committed tick 455 with no strict stop and zero browser logs. | **done**. |
| 4 | 48 | Physics looked new despite unavailable badge; version/indicator truth was unclear. | Match/automation/log output now separates Build `0.174`, compatible host orchestrator, requested engine, effective live/stopped authority, V2 authority version, committed tick and failure code. Quick Play labels CPU-v-CPU FL V2 explicitly. The real browser smoke showed `fl-v2`, committed tick 455 and `failureCode=none`, with zero logs. | **done**. |
| 5 | 48 | Permanently replace low-visibility ball with Nike Total 90 Aerow. | Default 3D match ball now uses a permanent custom high-contrast white/navy/gold procedural Aerow-inspired treatment with repeated spin cues and `TOTAL 90` marking. | **partial by design**: this is custom Aerow-inspired artwork, not an imported, licensed, pixel-exact Nike asset or logo. The visibility request is implemented; exact commercial artwork is intentionally not claimed. |
| 6 | 52 | Arsenal striker was isolated from halfway to goal with no runs or support; tester allowed reassessment after wider V2 refinements. | Live carrier-progress support floors now advance Invincibles strikers/wide players while preserving the asymmetric rest defence. Formation regression. | **done in contract; manual match proof** remains. |
| 7 | 53 | Asked whether replay interrupted a saved shot that stayed live. | The log shows a `shot-off-target` followed by `GOAL KICK`, not a save. The new replay gate additionally refuses `save-to-corner` while the ball remains in play and only permits it after confirmed goal-line exit/explicit stoppage. Replay regression. | **done**, and the original incident is forensically classified as a miss. |
| 8 | 56 | Chelsea attackers supported the carry but midfield vanished; also asked for roughly half-distance locomotion steps. | Conte carrier-relative floors now advance both CMs, wing-backs and front three. Visual locomotion uses cadence scale `2` and step-distance ratio `.5` at world speed `1`; it changes foot-plant presentation without slowing world movement. Formation and gait regressions. | **done in contract**; formation and animation feel still need subjective full-match tuning. |
| 9 | 56 | Referee needs a running animation. | Referee tracking now stores velocity, selects `refRun` while moving, and renders coordinated leg/arm run cycles. | **done in source; visual proof** remains. |
| 10 | 60 | Replay did not show the shot. | Every open-play `shot` records the latest launch anchor; a scoring replay chooses the same-team latest anchor and crops from 900 ms before it. Miss/corner-save replays also retain launch anchors. `tests/playtest-replay-keeper-cadence-regressions.mjs`. | **done**. |

## Exact remaining gaps from these logs

1. **Actual load optimisation:** first-click feedback and single-flight launch are fixed, but this batch did not reduce, benchmark, or prove a reduction in the match document's load time.
2. **Physical Bluetooth lifecycle:** automated fixtures cover the reported DualSense packets, disconnect, reconnect, and stale-input clearing. Only the real Mac/Bluetooth/controller lifecycle can close the hardware observation.
3. **Subjective full-match tuning:** the source contracts and focused/browser smokes close the deterministic defects, but formation support over 90 minutes, CPU free-kick outcomes, referee gait, shot/pass feel, shield feel, and final camera composition still need the user's normal full-match playtest. The prior Set-Piece browser visual smoke was green; that is not the same as a subjective final sign-off.
4. **Exact branded ball boundary:** the permanent ball is custom high-contrast Aerow-inspired artwork, not licensed or pixel-exact Nike artwork. No such licensing claim is made.

## Focused validation run

The following six focused suites were run together after the implementation batch:

- `tests/controller-reconnect-and-quick-play-launch.mjs`
- `tests/playtest-contact-input-regressions.mjs`
- `tests/playtest-replay-keeper-cadence-regressions.mjs`
- `tests/set-piece-camera-replay-integration.mjs`
- `tests/cpu-v-cpu-live-v2-authority.mjs`
- `tests/live-v2-period-handoff-integration.mjs`

Result: **31 tests passed, 0 failed** (including 17/17 controller/Quick Play checks). Browser smoke evidence additionally reached CPU-v-CPU committed tick 455 with the `fl-v2` badge and Single Player committed tick 202; both reported no fallback/failure and zero browser logs. The prior Set-Piece Suite visual browser smoke was green. This is focused implementation and browser-smoke evidence, not a substitute for the physical controller lifecycle or subjective full-match tuning listed above.
